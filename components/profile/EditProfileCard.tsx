'use client';

import { ProfileManager } from './ProfileManager';

/**
 * EditProfileCard - Wrapper component for profile editing
 * Uses ProfileManager for actual implementation
 */
export function EditProfileCard() {
  return <ProfileManager />;
}
